services:
  - type: web
    name: mutant-detector
    runtime: java
    buildCommand: ./mvnw clean package
    startCommand: java -jar target/mutant-detector-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
